diff --git a/node_modules/@capacitor-community/camera-preview/ios/Plugin/CameraController.swift b/node_modules/@capacitor-community/camera-preview/ios/Plugin/CameraController.swift
index a09e72a..9dbbf63 100644
--- a/node_modules/@capacitor-community/camera-preview/ios/Plugin/CameraController.swift
+++ b/node_modules/@capacitor-community/camera-preview/ios/Plugin/CameraController.swift
@@ -19,6 +19,11 @@ class CameraController: NSObject {
 
     var dataOutput: AVCaptureVideoDataOutput?
     var photoOutput: AVCapturePhotoOutput?
+    var videoWriter: AVAssetWriter?
+    var videoWriterInput: AVAssetWriterInput?
+    var isRecordingVideo = false
+    var videoRecordingURL: URL?
+    var videoRecordCompletionBlock: ((URL?, Error?) -> Void)?
 
     var rearCamera: AVCaptureDevice?
     var rearCameraInput: AVCaptureDeviceInput?
@@ -409,16 +414,83 @@ extension CameraController {
 
         let fileUrl = path.appendingPathComponent(fileName)
         try? FileManager.default.removeItem(at: fileUrl)
-        /*videoOutput!.startRecording(to: fileUrl, recordingDelegate: self)
-         self.videoRecordCompletionBlock = completion*/
+
+        guard !self.isRecordingVideo else {
+            completion(nil, CameraControllerError.invalidOperation)
+            return
+        }
+
+        // Grabación vía AVAssetWriter alimentado por el dataOutput ya existente:
+        // NO se toca la sesión de captura, así la preview nunca se ve afectada.
+        // El writer se crea perezosamente con el primer frame (dimensiones reales).
+        self.videoRecordingURL = fileUrl
+        self.videoRecordCompletionBlock = completion
+        self.isRecordingVideo = true
     }
 
     func stopRecording(completion: @escaping (Error?) -> Void) {
-        guard let captureSession = self.captureSession, captureSession.isRunning else {
-            completion(CameraControllerError.captureSessionIsMissing)
+        guard self.isRecordingVideo else {
+            completion(CameraControllerError.invalidOperation)
             return
         }
-        // self.videoOutput?.stopRecording()
+        self.isRecordingVideo = false
+
+        let writer = self.videoWriter
+        let input = self.videoWriterInput
+        let url = self.videoRecordingURL
+        let cb = self.videoRecordCompletionBlock
+        self.videoWriter = nil
+        self.videoWriterInput = nil
+        self.videoRecordingURL = nil
+        self.videoRecordCompletionBlock = nil
+
+        guard let w = writer, let i = input else {
+            cb?(nil, CameraControllerError.unknown)
+            completion(nil)
+            return
+        }
+        i.markAsFinished()
+        w.finishWriting {
+            if w.status == .completed {
+                cb?(url, nil)
+            } else {
+                cb?(nil, w.error ?? CameraControllerError.unknown)
+            }
+        }
+        completion(nil)
+    }
+
+    func _appendVideoFrame(_ sampleBuffer: CMSampleBuffer) {
+        if videoWriter == nil {
+            guard let url = videoRecordingURL,
+                  let format = CMSampleBufferGetFormatDescription(sampleBuffer) else { return }
+            let dims = CMVideoFormatDescriptionGetDimensions(format)
+            do {
+                let writer = try AVAssetWriter(outputURL: url, fileType: .mp4)
+                let settings: [String: Any] = [
+                    AVVideoCodecKey: AVVideoCodecType.h264,
+                    AVVideoWidthKey: Int(dims.width),
+                    AVVideoHeightKey: Int(dims.height),
+                ]
+                let input = AVAssetWriterInput(mediaType: .video, outputSettings: settings)
+                input.expectsMediaDataInRealTime = true
+                // Los buffers llegan en orientación de sensor (landscape): rotamos a retrato.
+                input.transform = CGAffineTransform(rotationAngle: .pi / 2)
+                guard writer.canAdd(input) else { return }
+                writer.add(input)
+                writer.startWriting()
+                writer.startSession(atSourceTime: CMSampleBufferGetPresentationTimeStamp(sampleBuffer))
+                videoWriter = writer
+                videoWriterInput = input
+            } catch {
+                isRecordingVideo = false
+                return
+            }
+        }
+        if let input = videoWriterInput, input.isReadyForMoreMediaData,
+           videoWriter?.status == .writing {
+            input.append(sampleBuffer)
+        }
     }
 }
 
@@ -497,6 +569,10 @@ extension CameraController: AVCapturePhotoCaptureDelegate {
 
 extension CameraController: AVCaptureVideoDataOutputSampleBufferDelegate {
     func captureOutput(_ output: AVCaptureOutput, didOutput sampleBuffer: CMSampleBuffer, from connection: AVCaptureConnection) {
+        if isRecordingVideo {
+            _appendVideoFrame(sampleBuffer)
+        }
+
         guard let completion = sampleBufferCaptureCompletionBlock else { return }
 
         guard let imageBuffer = CMSampleBufferGetImageBuffer(sampleBuffer) else {
diff --git a/node_modules/@capacitor-community/camera-preview/ios/Plugin/Plugin.swift b/node_modules/@capacitor-community/camera-preview/ios/Plugin/Plugin.swift
index 7fca23c..91ad51b 100644
--- a/node_modules/@capacitor-community/camera-preview/ios/Plugin/Plugin.swift
+++ b/node_modules/@capacitor-community/camera-preview/ios/Plugin/Plugin.swift
@@ -75,10 +75,15 @@ public class CameraPreview: CAPPlugin {
             }
 
             DispatchQueue.main.async {
-                if self.cameraController.captureSession?.isRunning ?? false {
-                    call.reject("camera already started")
-                } else {
-                    self.cameraController.prepare(cameraPosition: self.cameraPosition, disableAudio: self.disableAudio) {error in
+                // Si hay una sesión previa (viva o interrumpida), desmontarla y
+                // arrancar de cero en vez de rechazar con "camera already started":
+                // tras una interrupción (llamada, otro cliente de audio) isRunning
+                // queda en un estado que impedía reiniciar la preview.
+                if self.cameraController.captureSession != nil {
+                    self.cameraController.captureSession?.stopRunning()
+                    self.previewView?.removeFromSuperview()
+                }
+                self.cameraController.prepare(cameraPosition: self.cameraPosition, disableAudio: self.disableAudio) {error in
                         if let error = error {
                             print(error)
                             call.reject(error.localizedDescription)
@@ -86,9 +91,19 @@ public class CameraPreview: CAPPlugin {
                         }
                         let height = self.paddingBottom != nil ? self.height! - self.paddingBottom!: self.height!
                         self.previewView = UIView(frame: CGRect(x: self.x ?? 0, y: self.y ?? 0, width: self.width!, height: height))
+                        // Negro, no transparente: si la sesión no entrega frames se
+                        // ve negro (y no el fondo del sistema), y sirve de diagnóstico.
+                        self.previewView.backgroundColor = UIColor.black
                         self.webView?.isOpaque = false
                         self.webView?.backgroundColor = UIColor.clear
                         self.webView?.scrollView.backgroundColor = UIColor.clear
+                        if #available(iOS 15.0, *) {
+                            // En iOS 18/26 esta capa pinta blanco sobre la preview
+                            // nativa aunque el webview sea transparente.
+                            self.webView?.underPageBackgroundColor = UIColor.clear
+                            self.webView?.scrollView.subviews.forEach { $0.backgroundColor = UIColor.clear }
+                        }
+                        self.webView?.superview?.backgroundColor = UIColor.black
                         self.webView?.superview?.addSubview(self.previewView)
                         if self.toBack! {
                             self.webView?.superview?.bringSubviewToFront(self.webView!)
@@ -105,7 +120,6 @@ public class CameraPreview: CAPPlugin {
                         call.resolve()
 
                     }
-                }
             }
         })
 
@@ -122,14 +136,16 @@ public class CameraPreview: CAPPlugin {
 
     @objc func stop(_ call: CAPPluginCall) {
         DispatchQueue.main.async {
-            if self.cameraController.captureSession?.isRunning ?? false {
-                self.cameraController.captureSession?.stopRunning()
-                self.previewView.removeFromSuperview()
-                self.webView?.isOpaque = true
-                call.resolve()
-            } else {
-                call.reject("camera already stopped")
+            // Limpiar SIEMPRE, aunque la sesión ya no esté corriendo (p. ej. tras
+            // una interrupción del sistema): antes se rechazaba sin desmontar nada
+            // y el siguiente start() se encontraba la sesión zombi.
+            self.cameraController.captureSession?.stopRunning()
+            self.previewView?.removeFromSuperview()
+            self.webView?.isOpaque = true
+            if #available(iOS 15.0, *) {
+                self.webView?.underPageBackgroundColor = nil
             }
+            call.resolve()
         }
     }
     // Get user's cache directory path
@@ -286,6 +302,7 @@ public class CameraPreview: CAPPlugin {
         self.cameraController.stopRecording { (_) in
 
         }
+        call.resolve()
     }
     
     @objc func isCameraStarted(_ call: CAPPluginCall) {
